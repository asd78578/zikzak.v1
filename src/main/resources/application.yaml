spring:
  application:
    name: zikzak

  datasource:
    url: jdbc:postgresql://localhost:5432/zikzak
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081